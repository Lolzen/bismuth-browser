From a5aa576074856b0a20065f78e9bd5ee000af9018 Mon Sep 17 00:00:00 2001
From: Kiwi Browser <40272275+kiwibrowser@users.noreply.github.com>
Date: Wed, 11 Aug 2021 04:53:50 +0300
Subject: [PATCH 247/506] Remove code that was used to test that the build
 system was functional

---
 .../src/org/chromium/chrome/browser/PersonalizeResults.java    | 3 ---
 1 file changed, 3 deletions(-)

diff --git a/chrome/android/java/src/org/chromium/chrome/browser/PersonalizeResults.java b/chrome/android/java/src/org/chromium/chrome/browser/PersonalizeResults.java
index 85705bb8..621ea385 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/PersonalizeResults.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/PersonalizeResults.java
@@ -13,9 +13,6 @@ public class PersonalizeResults {
     public static void Execute(Tab tab) {
        final boolean shouldRewrapText = ContextUtils.getAppSharedPreferences().getBoolean("text_rewrap", false);
        final boolean shouldRemoveAmp = ContextUtils.getAppSharedPreferences().getBoolean("avoid_amp_websites", true);
-       if (tab != null && tab.getUrl().getSpec().startsWith("http://votreip.free.fr")) {
-          tab.getWebContents().evaluateJavaScript("alert('Welcome to Kiwi Browser test version')", null);
-       }
        if (shouldRemoveAmp && tab != null && IsSearchUrl(tab.getUrl().getSpec())) {
           tab.getWebContents().evaluateJavaScript(AMP_SCRIPT, null);
        }
