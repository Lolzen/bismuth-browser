From e86cd155405d1c1baaef66a3ab0d3a2c10fc41b3 Mon Sep 17 00:00:00 2001
From: Kiwi Browser <40272275+kiwibrowser@users.noreply.github.com>
Date: Sun, 15 Aug 2021 21:22:48 +0300
Subject: [PATCH 411/506] Update delete_committed_file.yml

---
 .github/workflows/delete_committed_file.yml | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/.github/workflows/delete_committed_file.yml b/.github/workflows/delete_committed_file.yml
index 7e9d5057..0718f2dc 100644
--- a/.github/workflows/delete_committed_file.yml
+++ b/.github/workflows/delete_committed_file.yml
@@ -21,6 +21,7 @@ jobs:
           git config --global user.name "Repository manager"
 
       - name: Deleting file from history
-        run: git filter-branch --force --index-filter "git rm --cached --ignore-unmatch ${{ github.event.inputs.filename }" --prune-empty --tag-name-filter cat -- --all
+        run: |
+          git filter-branch --force --index-filter "git rm --cached --ignore-unmatch ${{ github.event.inputs.filename }" --prune-empty --tag-name-filter cat -- --all
       
       - run: git push
