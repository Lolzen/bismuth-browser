diff --git a/chrome/browser/resources/extensions/item.ts b/chrome/browser/resources/extensions/item.ts
index eec862658c052..ad7ff0c9c1110 100644
--- a/chrome/browser/resources/extensions/item.ts
+++ b/chrome/browser/resources/extensions/item.ts
@@ -62,6 +62,8 @@ export interface ItemDelegate {
   recordUserAction(metricName: string): void;
   getItemStateChangedTarget():
       ChromeEvent<(data: chrome.developerPrivate.EventData) => void>;
+  getCopyProgressTarget():
+      ChromeEvent<(progress: chrome.developerPrivate.CopyProgress) => void>;
   showSiteSettings(id: string): void;
 }
 
@@ -116,6 +118,10 @@ export class DummyItemDelegate {
   getItemStateChangedTarget() {
     return new FakeChromeEvent();
   }
+
+  getCopyProgressTarget() {
+    return new FakeChromeEvent();
+  }
   showSiteSettings(_id: string) {}
 }
 
diff --git a/chrome/browser/resources/extensions/manager.css b/chrome/browser/resources/extensions/manager.css
index 18cf43dd6c300..d5d2f26462e22 100644
--- a/chrome/browser/resources/extensions/manager.css
+++ b/chrome/browser/resources/extensions/manager.css
@@ -73,3 +73,33 @@ extensions-item {
 #right {
   flex: 1 1 0;
 }
+
+#loadProgressOverlay {
+  align-items: center;
+  background: rgba(0, 0, 0, 0.5);
+  display: flex;
+  justify-content: center;
+  position: fixed;
+  z-index: 100;
+}
+
+#loadProgressCard {
+  background: var(--cr-dialog-background-color, #202124);
+  border-radius: 12px;
+  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.5);
+  max-width: 80%;
+  min-width: 220px;
+  padding: 20px 24px;
+}
+
+#loadProgressTitle {
+  color: var(--cr-primary-text-color);
+  font-size: 15px;
+  padding-bottom: 14px;
+}
+
+#loadProgressLabel {
+  color: var(--cr-secondary-text-color);
+  font-size: 13px;
+  padding-top: 12px;
+}
diff --git a/chrome/browser/resources/extensions/manager.html.ts b/chrome/browser/resources/extensions/manager.html.ts
index 1c4506dc4beaf..45b2e36c151b7 100644
--- a/chrome/browser/resources/extensions/manager.html.ts
+++ b/chrome/browser/resources/extensions/manager.html.ts
@@ -103,6 +103,18 @@ ${this.showLoadErrorDialog_ ? html`
   <extensions-load-error id="load-error" .delegate="${this.delegate}"
       @close="${this.onLoadErrorDialogClose_}">
   </extensions-load-error>`: ''}
+${this.showLoadProgressDialog_ ? html`
+  <div id="loadProgressOverlay">
+    <div id="loadProgressCard">
+      <div id="loadProgressTitle">Loading extension</div>
+      <cr-progress ?indeterminate="${!this.loadProgressTotal_}"
+          .value="${this.loadProgressCopied_}"
+          .max="${this.loadProgressTotal_ || 1}"></cr-progress>
+      <div id="loadProgressLabel">${this.loadProgressTotal_ ?
+          `Copying ${this.loadProgressCopied_} of ${this.loadProgressTotal_} files` :
+          'Preparing to copy...'}</div>
+    </div>
+  </div>`: ''}
 ${this.showInstallWarningsDialog_ ? html`
   <extensions-install-warnings-dialog
       @close="${this.onInstallWarningsDialogClose_}"
diff --git a/chrome/browser/resources/extensions/manager.ts b/chrome/browser/resources/extensions/manager.ts
index eccc0377d5f45..f13d70acecee5 100644
--- a/chrome/browser/resources/extensions/manager.ts
+++ b/chrome/browser/resources/extensions/manager.ts
@@ -2,6 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+import 'chrome://resources/cr_elements/cr_progress/cr_progress.js';
 import 'chrome://resources/cr_elements/cr_drawer/cr_drawer.js';
 import 'chrome://resources/cr_elements/cr_lazy_render/cr_lazy_render_lit.js';
 import 'chrome://resources/cr_elements/cr_toast/cr_toast_manager.js';
@@ -75,6 +76,7 @@ function compareExtensions(
 declare global {
   interface HTMLElementEventMap {
     'load-error': CustomEvent<Error|chrome.developerPrivate.LoadError>;
+    'load-progress': CustomEvent<boolean>;
   }
 }
 
@@ -148,6 +150,9 @@ export class ExtensionsManagerElement extends ExtensionsManagerElementBase {
 
       showDrawer_: {type: Boolean},
       showLoadErrorDialog_: {type: Boolean},
+      showLoadProgressDialog_: {type: Boolean},
+      loadProgressCopied_: {type: Number},
+      loadProgressTotal_: {type: Number},
       showInstallWarningsDialog_: {type: Boolean},
       installWarnings_: {type: Array},
       showOptionsDialog_: {type: Boolean},
@@ -185,6 +190,9 @@ export class ExtensionsManagerElement extends ExtensionsManagerElementBase {
   protected accessor narrow_: boolean = false;
   protected accessor showDrawer_: boolean = false;
   protected accessor showLoadErrorDialog_: boolean = false;
+  protected accessor showLoadProgressDialog_: boolean = false;
+  protected accessor loadProgressCopied_: number = 0;
+  protected accessor loadProgressTotal_: number = 0;
   protected accessor showInstallWarningsDialog_: boolean = false;
   protected accessor installWarnings_: string[]|null = null;
   protected accessor showOptionsDialog_: boolean = false;
@@ -238,6 +246,7 @@ export class ExtensionsManagerElement extends ExtensionsManagerElementBase {
     super.firstUpdated(changedProperties);
 
     this.addEventListener('load-error', this.onLoadError_);
+    this.addEventListener('load-progress', this.onLoadProgress_);
     this.addEventListener('view-enter-start', this.onViewEnterStart_);
     this.addEventListener('view-exit-start', this.onViewExitStart_);
     this.addEventListener('view-exit-finish', this.onViewExitFinish_);
@@ -264,6 +273,8 @@ export class ExtensionsManagerElement extends ExtensionsManagerElementBase {
 
       service.getItemStateChangedTarget().addListener(
           this.onItemStateChanged_.bind(this));
+      service.getCopyProgressTarget().addListener(
+          this.onCopyProgress_.bind(this));
     });
   }
 
@@ -692,6 +703,48 @@ export class ExtensionsManagerElement extends ExtensionsManagerElementBase {
     this.showLoadErrorDialog_ = false;
   }
 
+  private positionLoadProgressBound_ = () => this.positionLoadProgress_();
+
+  // A <dialog> centers itself in the layout viewport. On a phone the page is
+  // wider than the screen, so that lands outside what the user can see. Track
+  // the visual viewport instead and cover exactly the visible area.
+  private positionLoadProgress_() {
+    const overlay =
+        this.shadowRoot.querySelector<HTMLElement>('#loadProgressOverlay');
+    const viewport = window.visualViewport;
+    if (!overlay || !viewport) {
+      return;
+    }
+    overlay.style.left = `${viewport.offsetLeft}px`;
+    overlay.style.top = `${viewport.offsetTop}px`;
+    overlay.style.width = `${viewport.width}px`;
+    overlay.style.height = `${viewport.height}px`;
+  }
+
+  private onCopyProgress_(
+      progress: chrome.developerPrivate.CopyProgress) {
+    this.loadProgressCopied_ = progress.copied;
+    this.loadProgressTotal_ = progress.total;
+  }
+
+  private onLoadProgress_(e: CustomEvent<boolean>) {
+    this.showLoadProgressDialog_ = e.detail;
+    this.loadProgressCopied_ = 0;
+    this.loadProgressTotal_ = 0;
+    const viewport = window.visualViewport;
+    if (!viewport) {
+      return;
+    }
+    if (e.detail) {
+      setTimeout(() => this.positionLoadProgress_());
+      viewport.addEventListener('resize', this.positionLoadProgressBound_);
+      viewport.addEventListener('scroll', this.positionLoadProgressBound_);
+    } else {
+      viewport.removeEventListener('resize', this.positionLoadProgressBound_);
+      viewport.removeEventListener('scroll', this.positionLoadProgressBound_);
+    }
+  }
+
   protected onOptionsDialogClose_() {
     this.showOptionsDialog_ = false;
     this.shadowRoot.querySelector(
diff --git a/chrome/browser/resources/extensions/service.ts b/chrome/browser/resources/extensions/service.ts
index 848ddb82575bb..07d0e4be45e43 100644
--- a/chrome/browser/resources/extensions/service.ts
+++ b/chrome/browser/resources/extensions/service.ts
@@ -54,6 +54,10 @@ export class Service implements ServiceInterface {
     return chrome.developerPrivate.onItemStateChanged;
   }
 
+  getCopyProgressTarget() {
+    return chrome.developerPrivate.onCopyProgress;
+  }
+
   shouldIgnoreUpdate(
       extensionId: string,
       eventType: chrome.developerPrivate.EventType): boolean {
diff --git a/chrome/browser/resources/extensions/toolbar.ts b/chrome/browser/resources/extensions/toolbar.ts
index 163d17606312e..b4cf0f13a34e0 100644
--- a/chrome/browser/resources/extensions/toolbar.ts
+++ b/chrome/browser/resources/extensions/toolbar.ts
@@ -180,6 +180,9 @@ export class ExtensionsToolbarElement extends ExtensionsToolbarElementBase {
   }
 
   protected onLoadUnpackedClick_() {
+    // Unpacked extensions are copied into app storage before loading, which
+    // can take a while for large extensions. Show progress until it settles.
+    this.fire('load-progress', true);
     this.delegate.loadUnpacked()
         .then((success) => {
           if (success) {
@@ -190,6 +193,9 @@ export class ExtensionsToolbarElement extends ExtensionsToolbarElementBase {
         })
         .catch(loadError => {
           this.fire('load-error', loadError);
+        })
+        .finally(() => {
+          this.fire('load-progress', false);
         });
     chrome.metricsPrivate.recordUserAction('Options_LoadUnpackedExtension');
   }
diff --git a/chrome/common/extensions/api/developer_private.webidl b/chrome/common/extensions/api/developer_private.webidl
index ed195d634e4ef..f656dde4adfcb 100644
--- a/chrome/common/extensions/api/developer_private.webidl
+++ b/chrome/common/extensions/api/developer_private.webidl
@@ -573,6 +573,21 @@ dictionary DeleteExtensionErrorsProperties {
   ErrorType type;
 };
 
+// Progress while copying an unpacked extension into app storage.
+dictionary CopyProgress {
+  required long copied;
+  required long total;
+};
+
+// Listener callback for the onCopyProgress event.
+callback OnCopyProgressListener = undefined (CopyProgress progress);
+
+interface OnCopyProgressEvent : ExtensionEvent {
+  static undefined addListener(OnCopyProgressListener listener);
+  static undefined removeListener(OnCopyProgressListener listener);
+  static boolean hasListener(OnCopyProgressListener listener);
+};
+
 // Listener callback for the onItemStateChanged event.
 callback OnItemStateChangedListener = undefined (EventData response);
 
@@ -804,6 +819,8 @@ interface DeveloperPrivate {
   // Fired when a item state is changed.
   static attribute OnItemStateChangedEvent onItemStateChanged;
 
+  static attribute OnCopyProgressEvent onCopyProgress;
+
   // Fired when the profile's state has changed.
   static attribute OnProfileStateChangedEvent onProfileStateChanged;
 
diff --git a/tools/typescript/definitions/developer_private.d.ts b/tools/typescript/definitions/developer_private.d.ts
index 26e54ca663880..d78de5c45c233 100644
--- a/tools/typescript/definitions/developer_private.d.ts
+++ b/tools/typescript/definitions/developer_private.d.ts
@@ -494,7 +494,15 @@ declare global {
           Promise<boolean>;
       export function showSiteSettings(extensionId: string): Promise<void>;
 
+      export interface CopyProgress {
+        copied: number;
+        total: number;
+      }
+
       export const onItemStateChanged: ChromeEvent<(data: EventData) => void>;
+
+      export const onCopyProgress:
+          ChromeEvent<(progress: CopyProgress) => void>;
       export const onProfileStateChanged:
           ChromeEvent<(info: ProfileInfo) => void>;
       export const onUserSiteSettingsChanged:
