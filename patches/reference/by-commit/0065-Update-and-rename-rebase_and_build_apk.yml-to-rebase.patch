From e5c87044e6e571daf9bc42349ccb51629246c802 Mon Sep 17 00:00:00 2001
From: Kiwi Browser <40272275+kiwibrowser@users.noreply.github.com>
Date: Mon, 2 Aug 2021 04:33:02 +0300
Subject: [PATCH 065/506] Update and rename rebase_and_build_apk.yml to
 rebase.yml

---
 .github/workflows/rebase.yml               | 16 +++++++++++
 .github/workflows/rebase_and_build_apk.yml | 31 ----------------------
 2 files changed, 16 insertions(+), 31 deletions(-)
 create mode 100644 .github/workflows/rebase.yml
 delete mode 100644 .github/workflows/rebase_and_build_apk.yml

diff --git a/.github/workflows/rebase.yml b/.github/workflows/rebase.yml
new file mode 100644
index 00000000..950bc148
--- /dev/null
+++ b/.github/workflows/rebase.yml
@@ -0,0 +1,16 @@
+name: Rebase on new Chromium VERSION
+
+on:
+  workflow_dispatch:
+  push:
+    paths:
+      - 'CHROMIUM_VERSION'
+
+jobs:
+  build:
+    # The type of runner that the job will run on
+    runs-on: ubuntu-latest
+
+    steps:
+      - name: Rebasing
+        run: echo Rebasing files
diff --git a/.github/workflows/rebase_and_build_apk.yml b/.github/workflows/rebase_and_build_apk.yml
deleted file mode 100644
index a25f03dd..00000000
--- a/.github/workflows/rebase_and_build_apk.yml
+++ /dev/null
@@ -1,31 +0,0 @@
-name: rebase and build APK
-
-on:
-  workflow_dispatch:
-  push:
-    paths:
-      - 'CHROMIUM_VERSION'
-
-jobs:
-  build:
-    # The type of runner that the job will run on
-    runs-on: ubuntu-latest
-
-    strategy:
-      fail-fast: false
-      max-parallel: 4
-      matrix:
-        platform: [arm, arm64, x86, x64]
-
-    steps:
-      - name: Initializing build
-        run: echo Initializing build for platform ${{ matrix.platform }}
-
-  sign_and_upload:
-    runs-on: ubuntu-latest
-    needs: build
-
-    steps:
-      - name: Installing rclone
-        run: curl https://rclone.org/install.sh | sudo bash -s beta
- 
